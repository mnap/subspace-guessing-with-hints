[project]
name = "hint-threshold-analyzer"
version = "0.1.0"
description = "Analyze hint fractions needed for polynomial-time MinRank and RSD attacks."
requires-python = ">=3.13"

[tool.uv]
package = false
